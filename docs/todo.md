# 修正・作業メモ (TakumiNoguchi2004)

今後の実装内容
JP担当とEN担当のclaude codeを起動していることを認識してください. 以下の指示のうち担当範囲のものをやればいい. 

EN担当claude用 [完了]
全然実装が整備されていない
スマホフリックと同じ対応にする
JPの実装をほぼそのまま移植する(運指や削除操作など)
濁点の代わりに大文字化にすればいい(デフォルトを小文字にして, フリップで大文字, 悪くないと思う)
→ english.js を japanese.js と同型の状態機械で全面書き換え。運指テーブル(T/I/M/P組み合わせ)を
  行の並び順までJPと1:1対応させ(1行=あ行=T, 2行=か行=I, ...)、フリック/グー確定/削除ジェスチャーは
  同一パラメータで移植。濁点サイクルの代わりに大文字/小文字トグル(フリップ確定=即大文字、グー確定=常に
  小文字、確定後の追加フリップでトグル)。foldcore.js/fingereditor.js(旧fold実装)は他から未使用になったが
  指示により残置。i18n.js の guide.english / cam.enNotImpl を新実装に合わせて更新済み。


JP担当claude用
日本語変換機能の実装
漢字変換に限らず任意の変換と思えばいい, 
何らかの候補辞書のようなものを用意してそれを使うのが無難
変換操作はスペースに割り当てる. 変換開始と下への移動はスペース, 上への移動はフリップにすればよくて, 多分「、」フリップをshift spaceに相当する操作にしてしまえばいい
(p 下 フリップだと, 先にスペース判定が来るのでうまく機能しない)