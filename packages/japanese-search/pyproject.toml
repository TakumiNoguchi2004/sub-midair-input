[project]
name = "japanese-search"
version = "0.1.0"
description = "かな漢字変換 (SKK-JISYO ベース)"
requires-python = ">=3.12"
dependencies = [
    "midair-shared",
]

[tool.uv.sources]
midair-shared = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/japanese_search"]
